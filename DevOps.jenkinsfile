pipeline {
    agent any

tools {nodejs "node"}
    stages {
        stage('Build') {
            steps {
                echo "Building the Node.js application..."
                git 'https://github.com/PraneshASP/node-authentication-jwt-mongodb.git'
                bat 'npm install'
            }
        }
    }
}
